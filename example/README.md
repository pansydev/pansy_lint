Add `pansy_lint` as dependency to your pubspec.yaml

```yaml
dev_dependencies:
  pansy_lint:
```

Add a `analysis_options.yaml` to the root of you project.

```yaml
include: package:pansy_lint/analysis_options.yaml
```

or `analysis_options_package.yaml` for packages.
